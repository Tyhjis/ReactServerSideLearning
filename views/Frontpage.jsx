var React = require('react');
var Layout = require('./Layout.jsx');

class Site extends React.Component {
  render() {
    return (
      <Layout>
        <p>Moi kaikki</p>
      </Layout>
    );
  }
}

module.exports = Site;
